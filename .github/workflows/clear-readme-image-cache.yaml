name: "Clear github README image cache"
on:
  schedule:
  - cron: "30 * * * *"
  push:
    branches: [ "main" ]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: wow-actions/purge-readme@v1
        with:
          branch: main